import {generate} from "../Utilities/Generate";

export class LoaderScene extends Phaser.Scene {

    @generate
    static readonly KEY: string;

    preload(): void {
        // TODO: load assets for general use.
    }

    create(): void {

    }
}
